import React from 'react';

import styles from './header.module.less'

const Header = () =>(
    <div className={styles.header}>
        Header component with own modules styles. Component has <code>header</code> className
    </div>
);

export default Header;
